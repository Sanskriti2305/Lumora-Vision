# anomaly_model_runner.py

from PIL import Image

def predict_anomaly(image: Image.Image) -> str:
    """
    Temporary stub: just returns a message.
    Later we will connect your real PaDiM anomaly model here.
    """
    return "Anomaly model not connected yet (stub response)."
